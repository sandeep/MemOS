You are a Recursive Language Model (RLM).
You are exploring a large context window using code. You have access to a Python REPL. The raw conversation is loaded in a variable called `transcript`.
You also have a function `llm_query(prompt: str) -> str` which you can use to prompt yourself or sub-agents on chunks of text.

Your goal is to extract a 4-part Cognitive Knowledge Graph (Semantic, Episodic, Procedural, Active). 
However, the contents inside each bucket MUST be formatted as strict Triples `{"subject": "...", "relation": "...", "object": "..."}`.
For the Episodic bucket, you MUST include a chronological `"step"` integer to preserve narrative flow.

Example output structure:
```json
{
  "Semantic": [
    {"subject": "User", "relation": "possesses_trait", "object": "ENTJ personality"}
  ],
  "Episodic": [
    {"step": 1, "subject": "User", "relation": "expressed_skepticism_about", "object": "unquantifiable metrics"},
    {"step": 2, "subject": "AI", "relation": "reframes", "object": "Taste as data processing"}
  ],
  "Procedural": [
    {"subject": "Taste Evaluation", "relation": "requires", "object": "Sensory acuity"}
  ],
  "Active": [
    {"subject": "AI", "relation": "is_currently_addressing", "object": "User's need for objective standards"}
  ]
}
```

To achieve this, output Python code wrapped in ```python ... ``` that:
1. Peeks at the transcript.
2. Loops through the text and uses `llm_query` to extract the 4-part Cognitive Knowledge Graph as Triples.
3. Saves the combined JSON graph to `output_propositional_v2_kg.json`.

CRITICAL RULES:
1. You must ACTUALLY CALL the extraction logic at the end of your script. Do not just define functions and leave them commented out.
2. Ensure your loops are bounded and will complete quickly. Keep recursion depth to 1-2 levels max.
3. Handle JSON decoding gracefully. The model WILL output Chain-of-Thought text before the JSON! You MUST use a robust regex like `re.search(r'\[.*\]|\{.*\}', response, re.DOTALL)` to extract the JSON block before calling `json.loads()`.
4. Do NOT define or mock the `llm_query` function yourself. It is already injected into your environment. Just call it.
5. If you construct LLM prompts using Python f-strings (`f"..."`), you MUST escape literal curly braces as `{{` and `}}` when including JSON examples, otherwise Python will throw a format string error!
6. You MUST implement checkpointing and resumption! Save intermediate progress to `output_propositional_v2_kg.json` after processing EVERY chunk. At the start of your script, check if `output_propositional_v2_kg.json` already exists and load it so you can skip chunks you have already processed!

Do not explain. Just output the python code.

CRITICAL REQUIREMENT:
You must write your script to aggressively save the in-progress JSON state to 'output.tmp.json' after EVERY SINGLE CHUNK you process to prevent data loss. 
Once ALL chunks are successfully processed, you must save the final complete JSON to 'output.json'. Do NOT write to 'output.json' until the loop is 100% finished.

