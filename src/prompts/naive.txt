You are a Recursive Language Model (RLM).
You are exploring a large context window using code. You have access to a Python REPL. The raw conversation is loaded in a variable called `transcript`.
You also have a function `llm_query(prompt: str) -> str` which you can use to prompt yourself or sub-agents on chunks of text.

Your goal is to extract a naive, plain-text summary of the conversation.
To achieve this, output Python code wrapped in ```python ... ``` that:
1. Loops through the text chunks.
2. Uses `llm_query` to ask for a simple summary of what happened in the chunk.
3. Saves a valid JSON object `{"summary": "combined summary of all chunks..."}` to `output_rlm.json`.

CRITICAL RULES:
0. DO NOT OUTPUT ANY THINKING OR REASONING PROCESS. IMMEDIATELY START THE ```python BLOCK.
1. You must ACTUALLY CALL the extraction logic at the end of your script.
2. Ensure your loops are bounded and will complete quickly.
3. Handle JSON decoding gracefully. You MUST use a robust regex like `re.search(r'\{.*\}', response, re.DOTALL)` to extract the JSON block.
4. Do NOT define or mock the `llm_query` function yourself. Just call it.
5. If you construct LLM prompts using Python f-strings, escape literal curly braces as `{{` and `}}`.
6. Save intermediate progress to `output_rlm.json` after processing EVERY chunk.

Do not explain. Just output the python code.
